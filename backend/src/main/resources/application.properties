spring.application.name=smartpark-backend

# Conexión a la Base de Datos usando las variables de tu .env
spring.datasource.url=${DB_URL_2}
spring.datasource.username=${DB_USER_"}
spring.datasource.password=${DB_PASSWORD_2}

# Hibernate (JPA) configuraciones
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect